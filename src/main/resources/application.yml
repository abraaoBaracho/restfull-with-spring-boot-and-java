spring:
    application:
        name: rest-with-spring-boot-and-java
    datasource:
        url: jdbc:mysql://localhost:3306/rest_with_spring_udemy?useTimezone=true&serverTimezone=UTC
        username: root
        password: abraao123
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
        show-sql: false